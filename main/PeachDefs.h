#ifndef PEACH_DEFS_H
#define PEACH_DEFS_H
#define SAMPLE_FREQUENCY_HZ 50.0
#define MAX_PERIODIC_PER_STROKE 200
#define MAX_PACKET_SIZE 254

#ifndef MIN
#define MIN(x, y)  (((x) < (y))? (x) : (y))
#endif

#endif /* PEACH_DEFS_H */
